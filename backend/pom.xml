<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.visualizer</groupId>
    <artifactId>visualizer-backend</artifactId>
    <version>1.0.0</version>
    <name>Visualizer Backend</name>
    <description>
        Orchestrates code-execution tracing: content-addressed caching, rate
        limiting, and LLM narration in front of the language tracer services.
    </description>

    <properties>
        <!-- Java 21 LTS: the JDK installed here and pinned by the Docker image.
             Spring Boot 3.5.x supports 17-25, so bump this once a newer JDK is
             on the build and deploy hosts. -->
        <java.version>21</java.version>
        <azure-sdk-bom.version>1.2.35</azure-sdk-bom.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- The Azure SDK ships one BOM covering every client library, and
                 its whole job is to pin a set of versions that share a compatible
                 azure-core. Declaring azure-storage-blob without a version is
                 therefore deliberate: picking versions per-artifact is how you
                 end up with two azure-core lines on the classpath. -->
            <dependency>
                <groupId>com.azure</groupId>
                <artifactId>azure-sdk-bom</artifactId>
                <version>${azure-sdk-bom.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- In-memory bounded cache for traces and narration. Postgres/Redis
             are the multi-instance upgrade path; a single free-tier VM does not
             need them, and Caffeine keeps the app self-contained. -->
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>

        <!-- Spring Security plus the Nimbus JOSE plumbing, which is what signs
             and verifies the app's own access tokens (com.visualizer.auth).
             Nothing here talks to an external identity provider: the resource
             server is pointed at a local HMAC key rather than an issuer URI, so
             this brings the JWT encoder/decoder and the filter chain and no
             network dependency. BCrypt arrives with it too, via
             spring-security-crypto. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <!-- Durable tier of the trace cache. Caffeine still fronts it, so a blob
             is read only on a process-local miss and this client sits idle on
             the hot path. Version comes from azure-sdk-bom above. -->
        <dependency>
            <groupId>com.azure</groupId>
            <artifactId>azure-storage-blob</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
